import * as React from 'react';
import Form from "react-bootstrap/Form";
import {SequentialModel} from "./SequentialModel";


interface SequentialModalProps {
    node: SequentialModel;
}

const SequentialModal = (props: SequentialModalProps) => {
    return (
        <Form>
            <Form.Group>

            </Form.Group>
        </Form>
    )
}

export default SequentialModal;
